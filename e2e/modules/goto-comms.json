{
  "$module": "goto-comms",
  "description": "Navigate directly to the Kernl comms view and wait for it to load",
  "params": {},
  "actions": [
    {
      "comment": "Was `goto /` + wait for #app-ready + goto /#comms. Both halves were stale: #app-ready has never existed in this codebase, and the dashboard serves real SvelteKit routes, so the hash never resolved to a view. Comms is now an extension page (people-suite) mounted at /comms.",
      "$use": "goto-view",
      "params": { "path": "/comms", "settle": "1000" }
    }
  ]
}
