[
  {
    "name": "open hq dropdown",
    "actions": [
      {
        "comment": "Was a bare `goto http://localhost:3086/agents-flow` followed by a wait on `.hq-bar`. Unauthenticated the app redirects to /login or /setup, so `.hq-bar` never rendered and the wait burned its whole timeout. goto-view seeds the token + onboarding flags and waits for the shell first.",
        "$use": "goto-view",
        "params": { "path": "/agents-flow", "settle": "1000" }
      },
      {
        "type": "wait",
        "selector": ".hq-bar",
        "timeout": 30000
      },
      {
        "type": "wait",
        "value": "8000"
      },
      {
        "type": "click",
        "selector": ".hq-menu-trigger"
      },
      {
        "type": "wait",
        "value": "800"
      },
      {
        "type": "screenshot",
        "value": "hq-dropdown-open"
      }
    ],
    "expect": "hq-dropdown shows Create + View sections, the View section listing 5 toggles"
  }
]
