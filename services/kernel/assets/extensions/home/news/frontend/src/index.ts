/**
 * News extension — frontend page bundle entry (view "news").
 * Built by services/dashboard/scripts/build-ext-frontend.mjs into ../entry.js.
 */
import Page from "./Page.svelte";
import { createMount } from "$shared/mount";

export const mount = createMount(Page);
