// Auto-generated wrapper. The extension loader expects `createModule()` or
// a `default` export. Re-export the kernel module factory under that name.
import { createGoogleFlightsModule } from "../_module/index.js";

export function createModule() {
  return createGoogleFlightsModule();
}

export default createModule;
