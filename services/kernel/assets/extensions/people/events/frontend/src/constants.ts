// Timeline constants — the planner slice of
// services/dashboard/src/lib/constants.ts (TL_COLORS / TL_MONTHS).
// TL_COLORS lives in $shared/timeline so DayModal reads the same map.

export { TL_COLORS } from '$shared/timeline';

export const TL_MONTHS = ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];
